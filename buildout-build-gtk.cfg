[buildout]
extends = buildout.cfg
versions = versions
download-cache = downloads

[test]
extra-paths = ${gtk:path}

[scripts]
extra-paths = ${gtk:path}

[gtk]
recipe = tl.buildout_gtk

[versions]
tl.buildout-gtk = 0.2.2
zc.recipe.cmmi = 1.3.6
